# DCSOlympus Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-16

## Active Technologies
- In-memory only (no persistent storage). Context snapshots are ephemeral, created on-demand during polling cycles. (002-context-snapshot)
- TypeScript 5.8.2, Node.js 20 (ES2023 target, NodeNext module resolution) + None (uses existing BFIS infrastructure: `SnapshotReader`, `structured-logger`, `shared-schemas`) (003-hostility-awareness)
- In-memory only (no persistence - state maintained in `HostilityDetector` instance, lost on service restart) (003-hostility-awareness)
- TypeScript 5.8.2 (strict mode required per constitution) (004-bfis-llm-architecture)
- In-memory only (no persistent storage for MVP) (004-bfis-llm-architecture)

- TypeScript 5.8.2 / Node.js 20+ (ES2023, NodeNext module resolution) (001-bfis-snapshot-decoders)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint
Docker scripts are located in the root directory.
Always use the docker scripts to start and stop the stack.
Always Test by using the docker container.

## Code Style

TypeScript 5.8.2 / Node.js 20+ (ES2023, NodeNext module resolution): Follow standard conventions

## Recent Changes
- 004-bfis-llm-architecture: Added TypeScript 5.8.2 (strict mode required per constitution)
- 003-hostility-awareness: Added TypeScript 5.8.2, Node.js 20 (ES2023 target, NodeNext module resolution) + None (uses existing BFIS infrastructure: `SnapshotReader`, `structured-logger`, `shared-schemas`)
- 002-context-snapshot: Added TypeScript 5.8.2 / Node.js 20+ (ES2023, NodeNext module resolution)


<!-- MANUAL ADDITIONS START -->
Do not make use of emojis in at any time on any file.
Follow AGENTS.md for code style.
constitution is the highest authority.
DCSOlympus code is read-only.
<!-- MANUAL ADDITIONS END -->
