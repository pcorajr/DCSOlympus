# Multi-stage Dockerfile for BFIS service production build
# Base: node:20-bookworm-slim (Debian bookworm-slim)
#
# Build stage: install deps, compile TypeScript
# Production stage: copy build output, minimal deps
#
# TODO: Implement multi-stage build with proper dependencies and health check

